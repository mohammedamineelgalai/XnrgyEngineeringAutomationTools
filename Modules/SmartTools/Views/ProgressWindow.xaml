<Window x:Class="XnrgyEngineeringAutomationTools.Modules.SmartTools.Views.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Progression HTML - XEAT - v1.0.0 - R 2026-01-10 - By Mohammed Amine Elgalai - XNRGY CLIMATE SYSTEMS ULC"
        Icon="/Resources/XnrgyEngineeringAutomationTools.ico"
        Height="700" Width="900"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E2E"
        ResizeMode="CanResize"
        ShowInTaskbar="True"
        Topmost="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Styles/XnrgyStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <SolidColorBrush x:Key="XnrgyBlue" Color="#0078D4"/>
            <SolidColorBrush x:Key="DarkBg" Color="#1E1E2E"/>
            <SolidColorBrush x:Key="DarkBg2" Color="#252536"/>
            <SolidColorBrush x:Key="BorderColor" Color="#4A7FBF"/>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2A4A6F" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="TxtTitle" Grid.Column="0" 
                           Text="Progression"
                           FontSize="16" FontWeight="Bold" Foreground="White"
                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                
                <Button x:Name="BtnClose" Grid.Column="1" Content="X" 
                        Width="28" Height="28" FontWeight="Bold"
                        Background="#DC2626" Foreground="White" 
                        BorderThickness="0" Cursor="Hand"
                        Click="BtnClose_Click"/>
            </Grid>
        </Border>

        <!-- WebView2 pour le contenu HTML -->
        <Border Grid.Row="1" Background="#252536" Margin="8,4,8,8" CornerRadius="4"
                BorderBrush="#4A7FBF" BorderThickness="1">
            <wv2:WebView2 x:Name="WebViewContent" 
                          DefaultBackgroundColor="Transparent"/>
        </Border>

        <!-- COPYRIGHT BAR -->
        <Border Grid.Row="2" Style="{StaticResource CopyrightBarBorder}">
            <TextBlock Style="{StaticResource CopyrightTextStyle}" HorizontalAlignment="Center">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}{0} - {1} - {2} - {3} - By {4} - {5}">
                        <Binding Source="Progression"/>
                        <Binding Source="{StaticResource AppShortName}"/>
                        <Binding Source="{StaticResource AppVersion}"/>
                        <Binding Source="{StaticResource AppReleaseDate}"/>
                        <Binding Source="{StaticResource AppAuthor}"/>
                        <Binding Source="{StaticResource AppCompany}"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </Border>
    </Grid>
</Window>
