<Window x:Class="XnrgyEngineeringAutomationTools.Modules.VaultUpload.Views.VaultUploadModuleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Upload Module vers Vault - v1.0.0 - Released on 2025-12-31 - By Mohammed Amine Elgalai - XNRGY CLIMATE SYSTEMS ULC" 
        Height="950" Width="1550"
        MinWidth="1400"
        MinHeight="800"
        WindowStartupLocation="CenterOwner"
        WindowState="Maximized"
        Background="{DynamicResource WindowBackground}"
        Loaded="Window_Loaded"
        Closing="Window_Closing">

    <Window.Resources>
        <!-- ========== COULEURS THEME ========== -->
        <SolidColorBrush x:Key="XnrgyBlue" Color="#0078D4"/>
        <SolidColorBrush x:Key="XnrgyGreen" Color="#00D26A"/>
        <SolidColorBrush x:Key="XnrgyOrange" Color="#FF8C00"/>
        <SolidColorBrush x:Key="XnrgyRed" Color="#E81123"/>
        
        <!-- Theme Sombre (defaut) -->
        <SolidColorBrush x:Key="WindowBackground" Color="#1E1E2E"/>
        <SolidColorBrush x:Key="PanelBackground" Color="#252536"/>
        <SolidColorBrush x:Key="InputBackground" Color="#2D2D44"/>
        <SolidColorBrush x:Key="BorderColor" Color="#4A7FBF"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#888888"/>
        <SolidColorBrush x:Key="DataGridAltRow" Color="#2A2A3E"/>
        
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- ========== STYLE GROUPBOX MODERNE ========== -->
        <Style x:Key="ModernGroupBox" TargetType="GroupBox">
            <Setter Property="Background" Value="{DynamicResource PanelBackground}"/>
            <Setter Property="BorderBrush" Value="#4A7FBF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Background="#2A4A6F" CornerRadius="6,6,0,0" Padding="12,8"
                                    BorderBrush="#4A7FBF" BorderThickness="1,1,1,0">
                                <ContentPresenter ContentSource="Header" TextElement.Foreground="White" TextElement.FontWeight="Bold" TextElement.FontSize="14"/>
                            </Border>
                            <Border Grid.Row="1" Background="{TemplateBinding Background}" 
                                    BorderBrush="#4A7FBF" 
                                    BorderThickness="1,0,1,1"
                                    CornerRadius="0,0,6,6"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========== BOUTON PRIMAIRE - Glow Brillant ========== -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource XnrgyBlue}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#4A7FBF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1E90FF"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#00D4FF"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00D4FF" BlurRadius="30" ShadowDepth="0" Opacity="0.9"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#4A7FBF"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========== BOUTON SUCCES - Glow Brillant ========== -->
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{DynamicResource XnrgyGreen}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="#4A7FBF" BorderThickness="2"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="scaleTransform" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#32CD32"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#00FF7F"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00FF7F" BlurRadius="30" ShadowDepth="0" Opacity="0.9"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" Storyboard.TargetProperty="ScaleX" To="0.95" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" Storyboard.TargetProperty="ScaleY" To="0.95" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" Storyboard.TargetProperty="ScaleX" To="1.0" Duration="0:0:0.2">
                                                <DoubleAnimation.EasingFunction><ElasticEase Oscillations="1" Springiness="3"/></DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" Storyboard.TargetProperty="ScaleY" To="1.0" Duration="0:0:0.2">
                                                <DoubleAnimation.EasingFunction><ElasticEase Oscillations="1" Springiness="3"/></DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#4A7FBF"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========== BOUTON DANGER - Glow Brillant + Animation ========== -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{DynamicResource XnrgyRed}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="#4A7FBF" BorderThickness="2"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="scaleTransform" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF4444"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF6B6B"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#FF6B6B" BlurRadius="25" ShadowDepth="0" Opacity="0.8"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" Storyboard.TargetProperty="ScaleX" To="0.95" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" Storyboard.TargetProperty="ScaleY" To="0.95" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" Storyboard.TargetProperty="ScaleX" To="1.0" Duration="0:0:0.2">
                                                <DoubleAnimation.EasingFunction><ElasticEase Oscillations="1" Springiness="3"/></DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" Storyboard.TargetProperty="ScaleY" To="1.0" Duration="0:0:0.2">
                                                <DoubleAnimation.EasingFunction><ElasticEase Oscillations="1" Springiness="3"/></DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#4A7FBF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========== BOUTON WARNING - Glow Brillant + Animation ========== -->
        <Style x:Key="WarningButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{DynamicResource XnrgyOrange}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="#4A7FBF" BorderThickness="2"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="scaleTransform" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFA500"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FFD700"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#FFD700" BlurRadius="28" ShadowDepth="0" Opacity="0.85"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" Storyboard.TargetProperty="ScaleX" To="0.95" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" Storyboard.TargetProperty="ScaleY" To="0.95" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" Storyboard.TargetProperty="ScaleX" To="1.0" Duration="0:0:0.2">
                                                <DoubleAnimation.EasingFunction><ElasticEase Oscillations="1" Springiness="3"/></DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation Storyboard.TargetName="scaleTransform" Storyboard.TargetProperty="ScaleY" To="1.0" Duration="0:0:0.2">
                                                <DoubleAnimation.EasingFunction><ElasticEase Oscillations="1" Springiness="3"/></DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#4A7FBF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========== BOUTON SECONDAIRE - Glow Brillant ========== -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource InputBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="#4A7FBF"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2A5A8F"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#00D4FF"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00D4FF" BlurRadius="28" ShadowDepth="0" Opacity="0.85"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========== TEXTBOX MODERNE ========== -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource InputBackground}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#4A7FBF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0" VerticalScrollBarVisibility="Disabled"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource XnrgyBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- ========== STYLE DATAGRID MODERNE ========== -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#1E1E2E"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#4A7FBF"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="RowBackground" Value="#252536"/>
            <Setter Property="AlternatingRowBackground" Value="#2D2D44"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#4A7FBF"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
        </Style>

        <!-- Style Header DataGrid - fond bleu marine XNRGY avec texte blanc gras -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#4A7FBF"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="#3A5A7F"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
        </Style>

        <!-- Style Cell DataGrid -->
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#0078D4"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Style Row DataGrid -->
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#252536"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Style.Triggers>
                <Trigger Property="AlternationIndex" Value="1">
                    <Setter Property="Background" Value="#2D2D44"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#0078D4"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3D3D55"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>

        <!-- ========== SECTION 0: HEADER (Style Créer Module) ========== -->
        <Border Grid.Row="0" Background="#2A4A6F" Padding="15,12" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Logo + Titre + Description -->
                <Viewbox Grid.Column="0" Stretch="Uniform" MaxWidth="600" MaxHeight="80" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="#AEEA00" CornerRadius="8" Width="50" Height="50" Margin="0,0,12,0">
                            <TextBlock Text="&#x1F4E4;" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="📤 Upload Module - Vault Check-In v1.0.0" FontSize="20" FontWeight="Bold" Foreground="White"/>
                            <TextBlock FontSize="14" Foreground="#FFB74D" Margin="0,3,0,0" FontWeight="SemiBold">
                                <Run Text="💡 Upload des fichiers Inventor et documents vers Autodesk Vault avec les proprietes XNRGY automatiques"/>
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>
                </Viewbox>

                <!-- Centre: Statistiques (agrandies) -->
                <Viewbox Grid.Column="1" Stretch="Uniform" MaxWidth="500" MaxHeight="80" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20,0">
                    <Border Background="#2D2D44" CornerRadius="6" Padding="15,4"
                            BorderBrush="#4A7FBF" BorderThickness="2">
                        <StackPanel VerticalAlignment="Center">
                            <!-- Titre -->
                            <TextBlock Text="📈 Statistiques" Foreground="#FFD700" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                            <!-- Valeurs -->
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <StackPanel Margin="0,0,20,0" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock x:Name="TxtStatsTotal" Text="0" Foreground="White" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <TextBlock Text="📊 Total" Foreground="White" FontSize="11" HorizontalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Margin="0,0,20,0" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock x:Name="TxtStatsInventor" Text="0" Foreground="#FFD700" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <TextBlock Text="📐 Inventor" Foreground="#FFD700" FontSize="11" HorizontalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Margin="0,0,20,0" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock x:Name="TxtStatsNonInventor" Text="0" Foreground="#FFB74D" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <TextBlock Text="📄 Autres" Foreground="#FFB74D" FontSize="11" HorizontalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock x:Name="TxtStatsSelected" Text="0" Foreground="#90EE90" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <TextBlock Text="✅ Selection" Foreground="#90EE90" FontSize="11" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Viewbox>

                <!-- Droite: Connexion Vault + Propriétés -->
                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                    <!-- Status Connexion -->
                    <Border Background="#2D2D44" CornerRadius="4" Padding="10,6" Margin="0,0,0,6"
                            BorderBrush="#4A7FBF" BorderThickness="2">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse x:Name="VaultStatusIndicator" Width="10" Height="10" Fill="#E81123" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock x:Name="TxtVaultStatus" FontWeight="SemiBold" FontSize="12">
                                <Run Text="🗄️" Foreground="#00BFFF"/><Run x:Name="RunVaultName" Text=" Vault : --  /  " Foreground="White"/><Run Text="👤" Foreground="#FFB74D"/><Run x:Name="RunUserName" Text=" Utilisateur : --  /  " Foreground="White"/><Run Text="📡" Foreground="#90EE90"/><Run x:Name="RunStatus" Text=" Statut : Deconnecte" Foreground="White"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                    <!-- Propriétés détectées -->
                    <Border x:Name="PropertiesPanel" Background="#2D2D44" CornerRadius="4" Padding="10,6" Visibility="Collapsed"
                            BorderBrush="#4A7FBF" BorderThickness="2">
                        <StackPanel>
                            <!-- Titre du groupe -->
                            <TextBlock Text="🏷️ Propriétés Vault" Foreground="#00BFFF" FontWeight="Bold" FontSize="12" Margin="0,0,0,6"/>
                            <!-- Valeurs -->
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📋 Projet: " Foreground="White" FontSize="11"/>
                                <TextBlock x:Name="TxtProjectNumber" Text="" Foreground="#FFD700" FontWeight="Bold" FontSize="12"/>
                                <TextBlock Text=" | REF: " Foreground="White" FontSize="11" Margin="8,0,0,0"/>
                                <TextBlock x:Name="TxtReference" Text="" Foreground="#FFD700" FontWeight="Bold" FontSize="12"/>
                                <TextBlock Text=" | M: " Foreground="White" FontSize="11" Margin="8,0,0,0"/>
                                <TextBlock x:Name="TxtModule" Text="" Foreground="#FFD700" FontWeight="Bold" FontSize="12"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ========== SECTION 1: SELECTION MODULE ========== -->
        <Border Grid.Row="1" Background="#2A4A6F" CornerRadius="6" Padding="10" Margin="0,0,0,8"
                BorderBrush="#4A7FBF" BorderThickness="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MinWidth="200"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="TxtProjectPath" Grid.Column="0" Style="{StaticResource ModernTextBox}" Height="34"
                         BorderBrush="#4A7FBF" BorderThickness="2"/>
                
                <Button Grid.Column="1" Content="📋 Module" Style="{StaticResource SecondaryButton}" 
                        Background="#5B5FC7" Foreground="White"
                        Click="SelectModule_Click" Margin="8,0,0,0" Width="100" Height="34" FontSize="12" FontWeight="Bold"
                        BorderBrush="#4A7FBF" BorderThickness="2"/>
                <Button Grid.Column="2" Content="📁 Parcourir" Style="{StaticResource SecondaryButton}" 
                        Background="#0891B2" Foreground="White"
                        Click="BrowseFolder_Click" Margin="8,0,0,0" Width="110" Height="34" FontSize="12" FontWeight="Bold"
                        BorderBrush="#4A7FBF" BorderThickness="2"/>
                <Button Grid.Column="3" Content="🔧 Depuis Inventor" Style="{StaticResource SecondaryButton}" 
                        Background="#7C3AED" Foreground="White"
                        Click="GetFromInventor_Click" Margin="8,0,0,0" Width="155" Height="34" FontSize="12" FontWeight="Bold"
                        BorderBrush="#4A7FBF" BorderThickness="2"/>
                <Button Grid.Column="4" Content="🔍 Scanner" Style="{StaticResource PrimaryButton}" 
                        Click="ScanProject_Click" Margin="8,0,0,0" Width="100" Height="34" FontSize="12" FontWeight="Bold"
                        BorderBrush="#4A7FBF" BorderThickness="2"/>
            </Grid>
        </Border>

        <!-- ========== SECTION 2: LISTE FICHIERS UNIFIEE ========== -->
        <GroupBox Grid.Row="2" Style="{StaticResource ModernGroupBox}" Margin="0,0,0,8">
            <GroupBox.Header>
                <TextBlock Text="📄 Tous les Fichiers a Uploader" Foreground="White" FontWeight="Bold"/>
            </GroupBox.Header>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Controles: Boutons selection + Recherche + Filtres -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                    <Button Content="✅ Tout selectionner" Style="{StaticResource SuccessButton}" 
                            Click="SelectAll_Click" Height="30" MinWidth="140"/>
                    <Button Content="❌ Tout deselectionner" Style="{StaticResource DangerButton}" 
                            Click="DeselectAll_Click" Margin="6,0,0,0" Height="30" MinWidth="150"/>
                    
                    <!-- Recherche -->
                    <TextBlock Text="🔍" VerticalAlignment="Center" Margin="12,0,4,0" FontSize="14" Foreground="White"/>
                    <TextBox x:Name="TxtSearch" Style="{StaticResource ModernTextBox}" Width="180" Height="30" 
                             TextChanged="TxtSearch_TextChanged" ToolTip="Rechercher par nom"/>
                    
                    <!-- Filtre Extension (chargé dynamiquement) -->
                    <TextBlock Text="📋 Extension:" VerticalAlignment="Center" Margin="12,0,4,0" FontSize="11" Foreground="White" FontWeight="Bold"/>
                    <ComboBox x:Name="CmbExtension" Width="90" Height="30" VerticalContentAlignment="Center"
                              SelectionChanged="CmbExtension_SelectionChanged">
                        <ComboBoxItem Content="Tous" IsSelected="True"/>
                    </ComboBox>
                    
                    <!-- Filtre Statut -->
                    <TextBlock Text="📊 Statut:" VerticalAlignment="Center" Margin="12,0,4,0" FontSize="11" Foreground="White" FontWeight="Bold"/>
                    <ComboBox x:Name="CmbState" Width="100" Height="30" VerticalContentAlignment="Center"
                              SelectionChanged="CmbState_SelectionChanged">
                        <ComboBoxItem Content="Tous" IsSelected="True"/>
                        <ComboBoxItem Content="En attente"/>
                        <ComboBoxItem Content="Uploade"/>
                        <ComboBoxItem Content="Ignore"/>
                        <ComboBoxItem Content="Erreur"/>
                    </ComboBox>
                    
                    <!-- Categorie Vault -->
                    <TextBlock Text="📁 Categorie:" VerticalAlignment="Center" Margin="12,0,4,0" FontSize="11" Foreground="White" FontWeight="Bold"/>
                    <ComboBox x:Name="CmbCategory" Width="120" Height="30" DisplayMemberPath="Name" 
                              VerticalContentAlignment="Center" SelectionChanged="CmbCategory_SelectionChanged"/>
                    
                    <!-- Lifecycle State Vault -->
                    <TextBlock Text="🔄 State:" VerticalAlignment="Center" Margin="12,0,4,0" FontSize="11" Foreground="White" FontWeight="Bold"/>
                    <ComboBox x:Name="CmbLifecycleState" Width="130" Height="30" DisplayMemberPath="Name"
                              VerticalContentAlignment="Center"/>
                </StackPanel>

                <!-- DataGrid Unifie -->
                <DataGrid x:Name="DgFiles" Grid.Row="1" 
                          AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                          IsReadOnly="False"
                          Background="{DynamicResource PanelBackground}"
                          Foreground="{DynamicResource TextPrimary}"
                          GridLinesVisibility="Horizontal"
                          AlternatingRowBackground="{DynamicResource DataGridAltRow}"
                          RowHeight="32" SelectionMode="Single" SelectionUnit="FullRow"
                          BorderBrush="{DynamicResource BorderColor}" BorderThickness="2"
                          MouseLeftButtonUp="DataGrid_MouseLeftButtonUp">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="✅" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="40"/>
                        <DataGridTextColumn Header="📄 Nom du fichier" Binding="{Binding FileName}" Width="200" IsReadOnly="True"/>
                        <DataGridTextColumn Header="📐 Extension" Binding="{Binding FileType}" Width="70" IsReadOnly="True"/>
                        <DataGridTextColumn Header="📊 Taille" Binding="{Binding FileSizeFormatted}" Width="80" IsReadOnly="True"/>
                        <DataGridTextColumn Header="📁 Chemin complet" Binding="{Binding FullPath}" Width="*" IsReadOnly="True"/>
                        <DataGridTextColumn Header="🔄 Statut" Binding="{Binding Status}" Width="150" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <!-- ========== SECTION 3: COMMENTAIRE ========== -->
        <GroupBox Grid.Row="3" Style="{StaticResource ModernGroupBox}" Padding="8" Margin="0,0,0,8">
            <GroupBox.Header>
                <TextBlock Text="📝 Commentaire Check-In" Foreground="White" FontWeight="Bold"/>
            </GroupBox.Header>
            <TextBox x:Name="TxtComment" Style="{StaticResource ModernTextBox}" Height="36" 
                     TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                     Text="[Auto] Check-in effectue par XNRGY Engineering Automation Tools - Module Upload Module"/>
        </GroupBox>

        <!-- ========== SECTION 4: BARRE PROGRESSION + BOUTONS ========== -->
        <Border Grid.Row="4" Background="#2A4A6F" CornerRadius="8" Padding="15,12" Margin="0,0,0,8"
                BorderBrush="#4A7FBF" BorderThickness="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Barre de progression -->
                <Border Grid.Column="0" Background="#1E1E2E" CornerRadius="6" Padding="2" Height="40" Margin="0,0,15,0"
                        BorderBrush="#4A7FBF" BorderThickness="2">
                    <Grid>
                        <ProgressBar x:Name="ProgressBar" Minimum="0" Maximum="100" Value="0" Height="36"
                                     Foreground="{DynamicResource XnrgyGreen}" Background="Transparent" BorderThickness="0"/>
                        <TextBlock x:Name="TxtProgress" Text="0% - Pret" HorizontalAlignment="Center" VerticalAlignment="Center"
                                   FontSize="12" FontWeight="Bold" Foreground="White"/>
                    </Grid>
                </Border>

                <!-- Boutons action -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="BtnCheckIn" Content="▶️ UPLOAD" 
                            Style="{StaticResource SuccessButton}" Height="40" MinWidth="115"
                            Click="CheckIn_Click" Margin="0,0,8,0"/>
                    
                    <Button x:Name="BtnPause" Content="⏸️ PAUSE" 
                            Style="{StaticResource WarningButton}" Height="40" MinWidth="105"
                            Click="Pause_Click" Margin="0,0,8,0" IsEnabled="False"/>
                    
                    <Button x:Name="BtnStop" Content="⏹️ STOP" 
                            Style="{StaticResource DangerButton}" Height="40" MinWidth="95"
                            Click="Stop_Click" Margin="0,0,8,0" IsEnabled="False"/>
                    
                    <Button x:Name="BtnCancel" Content="❌ FERMER" 
                            Style="{StaticResource DangerButton}" Height="40" MinWidth="110"
                            Click="Cancel_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ========== SECTION 5: JOURNAL (en bas) ========== -->
        <Border Grid.Row="5" Background="{DynamicResource PanelBackground}" BorderBrush="#4A7FBF" 
                BorderThickness="2" CornerRadius="6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Header Journal -->
                <Border Grid.Row="0" Background="#2A4A6F" CornerRadius="6,6,0,0" Padding="12,8">
                    <Grid>
                        <TextBlock Text="📋 Journal des Opérations" FontSize="14" FontWeight="Bold" Foreground="White"/>
                        <Button Content="🗑️ Effacer" HorizontalAlignment="Right" 
                                Style="{StaticResource DangerButton}"
                                Padding="8,2" Click="ClearLog_Click" Height="28"/>
                    </Grid>
                </Border>
                
                <!-- Contenu Journal -->
                <RichTextBox x:Name="LogBox" Grid.Row="1" IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                             Background="{DynamicResource InputBackground}" Foreground="{DynamicResource TextPrimary}"
                             BorderThickness="0" FontFamily="Consolas" FontSize="14" Padding="10,5">
                    <FlowDocument>
                        <Paragraph>
                            <Run Text="[i] Module Upload initialisé - Prêt" Foreground="#4FC3F7"/>
                        </Paragraph>
                    </FlowDocument>
                </RichTextBox>
            </Grid>
        </Border>
    </Grid>
</Window>
