<Window x:Class="XnrgyEngineeringAutomationTools.Shared.Views.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{StaticResource WindowTitlePreview}" 
        Height="700" Width="900"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E2E">

    <Window.Resources>
        <!-- Couleurs -->
        <SolidColorBrush x:Key="XnrgyBlue" Color="#0078D4"/>
        <SolidColorBrush x:Key="XnrgyGreen" Color="#107C10"/>
        <SolidColorBrush x:Key="XnrgyOrange" Color="#FF8C00"/>
        <SolidColorBrush x:Key="DarkBg" Color="#1E1E2E"/>
        <SolidColorBrush x:Key="DarkBg2" Color="#252536"/>
        <SolidColorBrush x:Key="BorderColor" Color="#4A7FBF"/>
        
        <!-- Style Bouton - Glow Brillant -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2D2D44"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" 
                                BorderBrush="#4A7FBF" BorderThickness="2" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2A5A8F"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#00D4FF"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00D4FF" BlurRadius="28" ShadowDepth="0" Opacity="0.85"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#107C10"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="25,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" 
                                BorderBrush="#4A7FBF" BorderThickness="2" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#32CD32"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#00FF7F"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00FF7F" BlurRadius="30" ShadowDepth="0" Opacity="0.9"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0078D4" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="👁️" FontSize="28" VerticalAlignment="Center" Margin="0,0,15,0"/>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Prévisualisation de la Création" FontSize="20" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Vérifiez les informations avant de procéder" FontSize="12" Foreground="#B8D4E8" Margin="0,3,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Contenu principal -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="20">
            <StackPanel>
                
                <!-- Section Projet -->
                <Border Background="#252536" CornerRadius="8" Padding="20" Margin="0,0,0,15" 
                        BorderBrush="#4A7FBF" BorderThickness="2">
                    <StackPanel>
                        <TextBlock Text="📁 Informations Projet" FontSize="16" FontWeight="Bold" 
                                   Foreground="#FF8C00" Margin="0,0,0,15"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Numéro Projet:" Foreground="#888" Margin="0,5"/>
                            <TextBlock x:Name="TxtPreviewProject" Grid.Row="0" Grid.Column="1" Text="-" 
                                       Foreground="White" FontWeight="SemiBold" Margin="0,5"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Référence / Module:" Foreground="#888" Margin="0,5"/>
                            <TextBlock x:Name="TxtPreviewRefModule" Grid.Row="1" Grid.Column="1" Text="-" 
                                       Foreground="White" FontWeight="SemiBold" Margin="0,5"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Numéro Complet:" Foreground="#888" Margin="0,5"/>
                            <TextBlock x:Name="TxtPreviewFullNumber" Grid.Row="2" Grid.Column="1" Text="-" 
                                       Foreground="#0078D4" FontWeight="Bold" FontSize="16" FontFamily="Consolas" Margin="0,5"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Destination:" Foreground="#888" Margin="0,5"/>
                            <TextBlock x:Name="TxtPreviewDestination" Grid.Row="3" Grid.Column="1" Text="-" 
                                       Foreground="White" FontFamily="Consolas" FontSize="11" TextWrapping="Wrap" Margin="0,5"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Section Propriétés -->
                <Border Background="#252536" CornerRadius="8" Padding="20" Margin="0,0,0,15" 
                        BorderBrush="#4A7FBF" BorderThickness="2">
                    <StackPanel>
                        <TextBlock Text="👤 Propriétés iProperties" FontSize="16" FontWeight="Bold" 
                                   Foreground="#107C10" Margin="0,0,0,15"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Initiale Dessinateur:" Foreground="#888" Margin="0,5"/>
                            <TextBlock x:Name="TxtPreviewDessinateur" Grid.Row="0" Grid.Column="1" Text="-" 
                                       Foreground="White" FontWeight="SemiBold" Margin="0,5"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Initiale Co-Dessinateur:" Foreground="#888" Margin="0,5"/>
                            <TextBlock x:Name="TxtPreviewCoDessinateur" Grid.Row="1" Grid.Column="1" Text="-" 
                                       Foreground="White" FontWeight="SemiBold" Margin="0,5"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Date de Création:" Foreground="#888" Margin="0,5"/>
                            <TextBlock x:Name="TxtPreviewDate" Grid.Row="2" Grid.Column="1" Text="-" 
                                       Foreground="White" FontWeight="SemiBold" Margin="0,5"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Job Title:" Foreground="#888" Margin="0,5"/>
                            <TextBlock x:Name="TxtPreviewJobTitle" Grid.Row="3" Grid.Column="1" Text="-" 
                                       Foreground="#FF8C00" FontWeight="SemiBold" Margin="0,5"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Section Fichiers -->
                <Border Background="#252536" CornerRadius="8" Padding="20" Margin="0,0,0,15" 
                        BorderBrush="#4A7FBF" BorderThickness="2">
                    <StackPanel>
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="📄 Fichiers à Copier" FontSize="16" FontWeight="Bold" Foreground="#0078D4"/>
                            <TextBlock x:Name="TxtPreviewFileCount" Grid.Column="1" Text="0 fichiers" 
                                       Foreground="#888" FontSize="14" VerticalAlignment="Center"/>
                        </Grid>
                        
                        <!-- Liste des fichiers -->
                        <Border Background="#1E1E2E" CornerRadius="6" MaxHeight="250">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ItemsControl x:Name="LstPreviewFiles">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#2D2D44" CornerRadius="4" Padding="10,8" Margin="0,2">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="50"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="30"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding FileType}" Foreground="#FF8C00" 
                                                               FontWeight="Bold" FontSize="11"/>
                                                    <TextBlock Grid.Column="1" Text="{Binding OriginalFileName}" 
                                                               Foreground="#888" FontSize="11" TextTrimming="CharacterEllipsis"/>
                                                    <TextBlock Grid.Column="2" Text="➜" Foreground="#0078D4" 
                                                               HorizontalAlignment="Center"/>
                                                    <TextBlock Grid.Column="3" Text="{Binding NewFileName}" 
                                                               Foreground="White" FontWeight="SemiBold" FontSize="11" 
                                                               TextTrimming="CharacterEllipsis"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Section Actions -->
                <Border Background="#1A2A1A" CornerRadius="8" Padding="15" BorderBrush="#4A7FBF" BorderThickness="2">
                    <StackPanel>
                        <TextBlock Text="✅ Actions qui seront effectuées:" Foreground="#80C080" 
                                   FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <TextBlock Text="1. Création de la structure de dossiers (1-Equipment, 2-Floor, etc.)" 
                                   Foreground="#60A060" FontSize="12" Margin="15,3,0,0"/>
                        <TextBlock Text="2. Copie des fichiers avec renommage" 
                                   Foreground="#60A060" FontSize="12" Margin="15,3,0,0"/>
                        <TextBlock Text="3. Mise à jour des iProperties sur les fichiers Inventor" 
                                   Foreground="#60A060" FontSize="12" Margin="15,3,0,0"/>
                        <TextBlock Text="4. Mise à jour des paramètres du Top Assembly" 
                                   Foreground="#60A060" FontSize="12" Margin="15,3,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#1A1A28" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="⚠️ Vérifiez les informations avant de confirmer" 
                           Foreground="#FF8C00" FontSize="12" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="BtnCancel" Content="✕ Annuler" Style="{StaticResource ModernButton}" 
                            Margin="0,0,10,0" Click="BtnCancel_Click"/>
                    <Button x:Name="BtnConfirm" Content="✓ CONFIRMER ET CRÉER" Style="{StaticResource PrimaryButton}" 
                            Click="BtnConfirm_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
