<Window x:Class="XnrgyEngineeringAutomationTools.Shared.Views.XnrgyMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="XNRGY"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        SizeToContent="Height"
        Width="520">
    
    <Border Background="#1E1E1E" 
            BorderBrush="#4A7FBF" 
            BorderThickness="2" 
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header avec logo et titre -->
            <Border Grid.Row="0" Background="#252526" CornerRadius="8,8,0,0" Padding="16,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Logo XNRGY -->
                    <Image Grid.Column="0" 
                           x:Name="LogoImage"
                           Width="32" Height="32" 
                           Margin="0,0,12,0"
                           VerticalAlignment="Center"/>
                    
                    <!-- Titre -->
                    <TextBlock Grid.Column="1" 
                               x:Name="TitleText"
                               Text="XNRGY Engineering Automation"
                               Foreground="White"
                               FontSize="14"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"/>
                    
                    <!-- Bouton fermer -->
                    <Button Grid.Column="2" 
                            x:Name="CloseButton"
                            Content="✕"
                            Width="28" Height="28"
                            Background="Transparent"
                            Foreground="#808080"
                            BorderThickness="0"
                            FontSize="12"
                            Cursor="Hand"
                            Click="CloseButton_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#E81123"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Contenu principal -->
            <Grid Grid.Row="1" Margin="20,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Icone du type de message -->
                <Border Grid.Column="0" 
                        x:Name="IconBorder"
                        Width="48" Height="48" 
                        CornerRadius="24"
                        Margin="0,0,16,0"
                        VerticalAlignment="Top">
                    <TextBlock x:Name="IconText"
                               FontSize="24"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>
                
                <!-- Message -->
                <TextBlock Grid.Column="1" 
                           x:Name="MessageText"
                           Foreground="White"
                           FontSize="13"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"
                           LineHeight="20"/>
            </Grid>

            <!-- Boutons -->
            <Border Grid.Row="2" Background="#252526" CornerRadius="0,0,8,8" Padding="16,12">
                <StackPanel x:Name="ButtonPanel" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Right">
                    <!-- Boutons ajoutés dynamiquement -->
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
